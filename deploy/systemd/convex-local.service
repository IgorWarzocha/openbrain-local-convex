[Unit]
Description=OpenBrain Convex Local Deployment
After=network.target

[Service]
Type=simple
WorkingDirectory=%h/openbrain
ExecStart=/usr/bin/env bash -lc 'cd %h/openbrain && exec %h/openbrain/scripts/convex-dev-service.sh'
Restart=always
RestartSec=2

[Install]
WantedBy=default.target
